// functions are fun

function syntaxExample () {
    //code to run
}

function doMath (x, y) {
    let result = x * y;
    console.log(`${result}, ${x / y}, ${x + y}, ${x - y}`);
    return result;
}

//call or invoke 
doMath(10, 4);
